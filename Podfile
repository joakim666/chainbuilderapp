platform :ios, '10.3'
use_frameworks!

source 'https://github.com/CocoaPods/Specs.git'

def available_pods
  pod 'Neon', '~> 0.4.0'
  pod 'RealmSwift', '~> 3.0.2'
  pod 'Firebase/AdMob', '~> 4.0.0'
end

target 'chainbuilder' do
  available_pods
end

target 'chainbuilder-dev' do
  available_pods
end

target 'chainbuilder-demo' do
  available_pods
end

target 'chainbuilderTests' do
  available_pods
  pod 'Nimble', '~> 7.0.1'
end


